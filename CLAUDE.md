# build_video.py 実装上の制約（既知のffmpeg不具合と対策）

このプロジェクトの `build_video.py` を変更する際は、過去に実際に踏んだ以下の
ffmpeg特有の不具合を再発させないこと。いずれも「エラーなく完了する（exit code 0）が
出力内容が壊れている」タイプの不具合で、ログやexit codeだけでは検知できない。

## 1. `-loop 1 -t {duration} -i photo.png` は複数の入力フレームを供給する

`-loop 1` した静止画入力は、`image2` デマルサの既定フレームレート(25fps)で
複製フレームを供給する。例えば `duration=0.45` なら約11フレーム、`duration=4` なら
約100フレームが `zoompan` に渡る。

`zoompan` の `d` パラメータは「1入力フレームあたりの出力フレーム数」であり、
入力フレームが複数来るたびに `on` が0にリセットされてd フレームずつ再生成される。
これにより、意図した尺の何倍にも渡って**同じアニメーションが繰り返される**
（見た目には「フリーズしている」「同じ写真にさらにズームしている」ように見える）。

**対策**: `zoompan` の直後に必ず以下を続ける。

```
zoompan=...:d={frames}:s={w}x{h}:fps={fps},trim=start_frame=0:end_frame={frames},setpts=PTS-STARTPTS,fps={fps}
```

- `trim` で意図したフレーム数ちょうどに強制的に切り詰める
- `setpts=PTS-STARTPTS` でタイムスタンプを0から振り直す
- 末尾の `fps={fps}` を忘れないこと（後述の2番目の問題とは別に、trim後は
  フレームレートのメタ情報が失われ、後段の `xfade` が
  "First input link main frame rate ... do not match" で失敗する）

## 2. 連鎖した `concat=n=2` を何度も繋いだ後に `xfade` を続けると、そこから先が
   サイレントに欠落する

`[acc1][v2]concat=n=2...[acc2]; [acc2][v3]concat=n=2...[acc3]; ...` のように
2入力の `concat` を何度も鎖状に繋いだ結果を、その後 `xfade` の入力に使うと、
**スライド枚数や1枚あたりの尺がある程度を超えたところで、xfade以降のフレームが
無言でdrop（`drop=`ログに出る）され、映像がそこで止まったまま進まなくなる**。
これは `concat` が2入力かどうかに関係なく、`xfade` の入力側に「多段に連鎖した
中間concat結果」を渡すこと自体が引き金になる（実測で確認済み。原因はffmpeg内部の
バッファ/フレーム同期の制限と推測されるが、ffmpeg側の一般的なオプションでは
回避できなかった）。

再現条件の目安（fps=30の場合）：ハードカットのみの区間が概ね6秒
（頻度や写真の長さにより変動）を超えて蓄積した状態で `xfade` に接続すると発生する。
写真の表示時間を延ばす変更（例: 0.45秒→0.75秒）だけでも、同じスライド枚数構成のまま
この閾値を超えて壊れることがあるため、**「動作確認済みだから安全」とは言えない**。

**対策（`build_filter_complex` の `flush_pending()` で実装済み）**:

1. ハードカットが連続する区間は1つずつ `concat=n=2` を鎖状に繋がず、
   `[v1][v2][v3]...concat=n=N` のように**1回の多入力concatにまとめる**。
2. すべての `xfade=...` の出力には**直後に `,fps={fps}` を必ず付ける**
   （`filmstrip` 遷移で使う `xfade` も含む）。`settb=1/{fps}` だけでは不十分。

この2つを両方満たさないと不具合が再発する（どちらか一方だけでは直らないことを
実験で確認済み）。`build_filter_complex` 以外の場所（`collage` 等）で同様の
concat/xfadeチェーンを新たに書く場合も、同じ対策を適用すること。

## 検証方法（重要）

上記の不具合はいずれも **exit code 0・ログにエラー文字列なし・
`ffprobe -show_entries format=duration` すら正常値を返すことがある**
（xfadeのoffset計算が総尺ベースで、実際のフレーム欠落とは無関係に決まるため）。
そのため、zoompan/concat/xfadeのフィルタグラフ構築ロジックを変更した後は、
**必ず以下のいずれかで実際にフレームが欠落していないか確認する**こと。
「レンダリングが完了した」「ffprobeの尺が合っている」だけでは不十分。

```sh
# 全編を強制デコードしてffmpeg自身の警告(drop=など)を確認
ffmpeg -v warning -i output.mp4 -f null -

# fps=10〜20程度にダウンサンプルしたフレーム列の差分を見て、
# シーンチェンジ間隔が異常に空いていないか(=止まっていないか)を確認
ffmpeg -y -i output.mp4 -vf "fps=15" /tmp/check/f_%04d.png
# → 連続フレームの平均輝度差分(numpy)を取り、スパイク間隔の最大値が
#   想定スライド長(+トランジション)を大きく超えていないか確認する
```

## その他の既定値

- `zoompan_expr()` の停止区間（動きが止まってから次のスライドに切り替わるまでの間）
  は既定 `1.0秒`（`default_hold`）。ユーザー要望で変更したもので、意図的に長め。
  写真の表示時間がこれより短いスライドでは、停止区間が優先され動き（ease）がほぼ
  見えなくなる（`frames - 1` までしか停止区間を取らない安全弁はあるが、
  疾走感のある短尺スライドショーでは実質的に動きがなくなる点に注意）。
